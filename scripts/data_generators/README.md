# Sample Data Generators

Ad-hoc, one-off scripts used to generate the sample files in `sample_data/`.
They are dev utilities only — not imported by the app and not run by any
build/deploy/test step. Some contain machine-specific paths from when they
were last used (e.g. a hardcoded Windows desktop path) and may need editing
before re-running.
